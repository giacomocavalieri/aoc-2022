(ns advent.days.day-2-test
  (:require [clojure.test :refer [deftest testing is]]
            #_[advent.days.day-2 :refer [part-a part-b input sample-input]]))

#_(deftest part-a-test-sample
    (testing "Day 1 solution on sample input"
      (is (= nil (part-a sample-input)))))

#_(deftest part-b-test-sample
    (testing "Day 1 solution on sample input"
      (is (= nil (part-b sample-input)))))

#_(deftest part-a-test
    (testing "Day 1 solution on sample input"
      (is (= nil (part-a input)))))

#_(deftest part-b-test
    (testing "Day 1 solution on sample input"
      (is (= nil (part-b input)))))
